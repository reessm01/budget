{
	"name": "static_src",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"compile:css": "sass budget/templates:budget/static/compiled/css --style=compressed --no-source-map",
		"compile:css:init": "sass budget/templates:budget/static/compiled/css --style=compressed",
		"compile:css:watch": "sass --watch budget/templates:budget/static/compiled/css --style=compressed",
		"compile:ts": "tsc",
		"compile:ts:watch": "tsc --watch",
		"deploy": "npm i && npm run compile:css && npm run compile:ts",
		"init": "npm i && npm run compile:css:init && compile:ts"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bootstrap": "^4.5.0",
		"typescript": "^3.9.3",
		"sass": "^1.26.5"
	}
}
